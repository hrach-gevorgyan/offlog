# Offlog — Goal

A task manager any non-technical person can pick up and use, with zero
knowledge of backends, servers, or databases required. Install the PC app
from a website, install the Android app from Google Play, use either one
completely on its own. When someone has both, the PC acts as the host and
they sync automatically over home Wi-Fi — private, not public, secure
enough on its own.

A small, trusted, co-located group (family, a small team, one office) can
also share one local workspace by connecting their own devices to the same
host — a shared board, not individual accounts or permissions.

The core app stays free, always, with no feature ever paywalled. It's
maintained by me; once stable, future improvements can come from community
contributions. Because it's open source and self-hosted, anyone can also
run their own fork, or a business can self-host their own internal
version — same app, their own server, their own data.

Integrations and automation are deliberately not part of the free core —
automation especially, since building a workflow engine is exactly the
feature-creep this project exists to avoid. Either can come from community
contributions or commissioned custom work for a specific client, never as
core scope.

Phone-as-host, remote/away-from-home sync, and per-user accounts or
permissions are explicitly not part of this goal — the PC is the host,
sync stays local, and the shared workspace has no individual boundaries.

---

This file states *why* and *what*, deliberately not *when* or *by what
version* — that belongs in ROADMAP.md, which already tracks current status.
For anything this file implies but doesn't resolve (e.g. what the PC
standalone app should actually be built with), see QUESTIONS.md rather
than assuming an answer here.
