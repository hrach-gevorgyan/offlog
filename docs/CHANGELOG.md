# Offlog — Changelog

The single source of truth for version history — this used to be duplicated
across `TECH.md` (technical detail) and the root `README.md` (short
highlights), which had drifted in coverage (TECH.md's table was more
complete). Consolidated here 2026-07 as part of the documentation cleanup;
both `README.md` and `docs/TECH.md` now link here instead of keeping their
own copy. New entries go at the top, one row per released version.

This table keeps the newest ~8 releases in full detail. Older releases are
compressed to one line each in [CHANGELOG-ARCHIVE.md](CHANGELOG-ARCHIVE.md)
(git still has every full diff) — split out 2026-07 once this file's
default-loaded size grew large enough to matter. When adding a new entry,
move the oldest row here into the archive (compressed to one line) so this
table stays roughly the same length release over release.

| Version | Changes |
|---|---|
| **4.6.0** | **Appearance + power-user pass** (A11, B21, B11, B16, B19) — merged from two originally-separate rows (owner request) into one release. **B21**: theme is now Light/Dark/System (was a boolean toggle) — System follows `prefers-color-scheme` live via a watcher, with one-time migration from the old `dark` localStorage key so an explicit prior choice isn't silently flipped. **B11**: a High Contrast toggle, layered on top of Light/Dark via `body.high-contrast`/`body.dark.high-contrast`, raising border/text contrast only (not the meaning colors). **B16**: custom fields (text/number/date/select) — shipped **global**, not per-project as originally scoped, after owner feedback mid-build that per-project fields with inline add/remove in `CardDetail` invited sprawl; now managed once from Settings → Organize → Manage Custom Fields, stored as a single `meta:custom_fields` doc, with `CardDetail` capping visible fields to 3 by default (a "show more" toggle) so cards don't balloon. **B19**: bulk actions in List — also revised twice from the original design: no shift-click range-select (dropped), row checkboxes only appear in an explicit "Select" mode merged into the existing Columns dropdown (a toggle switch, not a separate always-visible button), custom-styled checkboxes (not the native browser box), and the bulk bar trimmed to move-to-status / change-priority / add-tag only (no remove-tag, no bulk delete). **A11**: audited every mutation call site touched by this release (custom fields, bulk actions) for the try/catch+`showError()` invariant, plus a spot-check of existing surfaces — no gaps found |
| **4.5.0** | **Focus view (B35), draft.** New global nav item beside Dashboard/Agenda. Not the originally-scoped auto-computed priority list — that design was built, then rejected live by the owner ("not making sense, we need a really focus space") for feeling too passive/similar to Agenda. Replaced same-session with a **daily commitment lock**: pick up to 3 open tasks each day into a commitment; only those show until each is done or the day rolls over. The picker ranks candidates (pinned > overdue > due-soon > priority) but round-robins across those reason-buckets so the 3 suggestions are a genuine spread rather than "the 3 most overdue tasks" every morning; suggested rows are visually highlighted and labeled with a colored "why" chip (Pinned/Overdue/Due soon/High priority). The lock itself (`{date, taskIds}`) lives in `localStorage['offlog_focus_lock']`, deliberately not a PouchDB doc — ephemeral, per-device, unsynced. Commit button is a sticky footer, not buried at the bottom of a scrolling list. Still an open draft: add-task-from-Focus, a Dashboard link, and a Samsung-Daily-Brief-style summary card are confirmed next steps, not yet built (see ROADMAP.md's B35) |
| **4.4.2** | **Maintenance pass** (second run, after v4.4.0/v4.4.1 shipped, per the owner-set schedule in MAINTENANCE.md). Phase 0/1 baseline and analysis found the codebase largely clean — same conclusion as the first pass; approved fixes: `QuickAdd.svelte`'s `doAdd()` had no try/catch/`showError()` around `createTask()` (the one real gap in the otherwise-audited invariant — a failed write left "saving" stuck true forever with no error shown), and `App.svelte`'s `setView()` write (now-vestigial `default_view` persistence, per A27) got a documented silent catch, matching the existing `rescheduleAll().catch(() => {})` "fire and forget" pattern rather than a misleading error toast for a background write that doesn't affect the already-succeeded view switch. `npm audit`: unchanged (20 vulnerabilities, all dev/build tooling, none reach the shipped app). No dead code, no unused deps, cache-invalidation coverage confirmed complete |
| **4.4.1** | Light patch (A29, owner-reported live): also fixed the B12 "remind on due date" checkbox rendering stacked/centered instead of inline — the generic `label { flex-direction: column }` rule was silently overriding its intended row layout. **A29**: `describeSyncError()`'s network-failure message reworded from "Cannot reach sync server" to "…check you're on the same network/WiFi it runs on" — a device on the wrong WiFi (the sync URL is a LAN IP) shows an empty/default-seeded app in exactly this situation, easy to mistake for lost data without the message saying why |
| **4.4.0** | Reminder-scheduling release (A12, B12) — built the auto-reminder feature under a fresh audit of `notifications.ts`, per the sequencing rationale. **B12**: `CardDetail` gets a "Remind me on the due date" checkbox that derives `reminder_at` from `due_date` + a configurable default time (Settings → Notifications, default 9:00 AM) instead of requiring the exact date+time picked twice; recomputes live as `due_date` changes, disables the manual reminder field while on. New `TaskDoc.remindOnDue` field. **A12**: audited DST/timezone handling — confirmed by construction to be safe (`reminder_at` is an absolute epoch instant; every delay computed from it is plain epoch-ms arithmetic with no local-time component for a DST transition to corrupt). Found a real bug instead: `catchUpWeb()` left a reminder that missed the 1-hour catch-up window (app closed for hours/days) dangling forever — never fired, never cleared, silently "active" indefinitely. Fixed to explicitly clear a too-stale reminder, with regression coverage in new `tests/notifications.test.ts` |
| **4.3.0** | Storage-pressure release (A17, B14) — both touch the same Settings → Data screen and the same `navigator.storage.estimate()` call. **B14**: explains what "quota" actually means (a browser-assigned ceiling based on free disk space, not an Offlog-imposed limit) with a plain informational note in the common case. **A17**: once usage crosses 80% of quota, that note switches to an actual warning (amber, matches the existing due-soon color token) pointing at Maintenance's cleanup tools or freeing device storage — verified live by temporarily faking `navigator.storage.estimate()` to 93% usage. At personal-task-list scale this essentially never fires in practice, so the default stays a quiet fact rather than a persistent nag |
| **4.2.0** | Sync + device-identity release (A16, B13, B5, B22). **B22**: `Source` widened from a fixed `'pc'/'pc2'/'mobile'` enum to a free-form per-device name (`config.ts`'s `getDeviceName()`/`setDeviceName()`, editable in Settings → Sync), generated once as "PC"/"Android phone" on first run. **B5**: `CardDetail`'s history panel now shows the device name + relative time (`timeAgo()`) per entry, and Settings → Sync gets a "Devices seen recently" list (`getDeviceLastSeen()`, scans the most recent 500 changelog entries). **B13**: an explicit Sync on/off toggle in Settings — pausing cancels the live replication (`cancelSync()`) without touching the configured URL, and the `online`-event auto-resync now respects the toggle too. **A16**: there's no CI-reachable CouchDB to genuinely drop mid-replication against, so this became deterministic coverage instead — `tests/sync.test.ts` covers `describeSyncError()`'s auth/not-found/network classification and `attachSyncHandlers()`'s settle-once guard (the same dedup A5 shipped to stop two concurrent replications, now verified against an error-then-paused race too), plus confirming `startSync()` never calls `db.sync()` at all when the new pause toggle is off |
| **4.1.0** | The "3 widgets" release (A15, B20, B31). **A15**: `tests/modalStack.test.ts` — first automated coverage for the history-backed overlay stack (LIFO order, `discardTop()` vs `requestClose()`), the pure-JS module that's underpinned every overlay/back-button interaction since v3.7.0 with zero tests until now. **B20 + B31**: two new home-screen widgets — a read-only Agenda widget (next 3 due tasks) and a Project list widget (up to 4 projects, each row opens straight into it). Both needed more than "reuse the existing widget plumbing" turned out to mean: task/project data lives entirely in the WebView's JS-side PouchDB, invisible to native code, so there was no native DB a periodic timer could poll. Built a small shared bridge instead — `OffologWidgetPlugin.java` (an app-local Capacitor plugin, registered directly in `MainActivity`, not an npm package) plus `src/lib/widgetBridge.ts`, which pushes fresh data after every store reload rather than polling. Both widgets deep-link back into the app via `com.offlog.app://agenda` and `com.offlog.app://project?id=<id>`, resolved in `App.svelte` alongside the existing Quick Add widget's link — cold-start deep links now correctly take priority over restoring the last-open view |

Older releases (v4.0.0 and earlier): [CHANGELOG-ARCHIVE.md](CHANGELOG-ARCHIVE.md).
