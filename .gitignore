node_modules/
dist/
.env.local
.env.*.local
*.local
.DS_Store
Thumbs.db

# Local Claude Code tool permission cache -- not app config. Its recorded
# permission entries capture real command strings, which have included a
# live credential. *.local above doesn't catch it: the file ends in .json.
.claude/settings.local.json
offlog-app/node_modules/
offlog-app/dist/
offlog-app/.env.local
offlog-app/android/.gradle/
offlog-app/android/app/build/
offlog-app/android/build/

# Screenshots/exports for store listings, landing page, etc -- generated,
# never part of the shipped app or its source.
/brand kit/

# Personal build narrative -- kept locally, not published
/docs/archive/story.md

# Mesh-sync investigation: the spike and its scenario matrix. Kept locally
# as the evidence behind decisions.md's mesh entry, not published -- the
# decision and its reasoning are in the docs, which is what a reader needs.
/offlog-desktop/scripts/mesh-spike/
